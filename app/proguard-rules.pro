# Dialed :app ProGuard/R8 rules.
# Release minify is OFF today; this file exists so the release buildType's
# proguardFiles(...) reference resolves once Phase 6 enables shrinking.
# Add keep rules for Play Billing / Data Layer models when minify is turned on.
